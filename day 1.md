# Day-1
data engineer journey
# Day 1 - [installation and data types]

## What I Learned
- I learned that python follows a set of instructions in order, best annalogy was a recipe book
- I learned the difference between strings(woords) and intigers(numbers) and that there are specific methods to change one to the other
- I learned of the third type which are floats(decimals) main difference between floats and intigers is the decimal point

## Challenges Faced
- figuring out which program to install to get started seemed to just depend on what you wanted to use. I finally landed on vs code and was able to make an environment but still unsure on what a sub environment
- or repo does

## Code Highlights
```python
# int() str() float(), these are what is used to change each type to the other
strip(), allows you to control the spacing, I think this is how forms online or passwords control that
split() seperates each word into its own segment

Tomorrow's Goals
[x] install python and vs code
[x] read at least 1 chapter of automate the boring stuff(read 2)
